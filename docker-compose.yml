services:
  app:
    build: .
    container_name: football_events_app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - vendor_data:/app/vendor
    working_dir: /app

volumes:
  vendor_data:
